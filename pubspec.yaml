name: CiYing
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_svg: ^0.19.0
  http: ^0.12.0+2
  video_player_controls: ^2.1.6
  video_player_web: ^0.1.4
  flutter_login: ^1.0.14
  protobuf:
  grpc:
  base_x: ^1.0.1
  device_info: ^0.4.0+4
  fixnum: ^0.10.11  #fix flutter int64 https://github.com/PomCloud/gateway/issues/3
  shared_preferences: ^0.5.3+4






dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    
# fonts:
#   - family: appIconFont
#     fonts:
#     - asset: assets/fonts/iconfont.ttf
